<?xml version="1.0" encoding="utf-8"?>
<!--
    Voice-only IME panel (v1.0.7), Google-voice-style: a red ring around the
    mic that expands with the input level (invisible when silent), status/hint
    text, and a small white gear. Tapping anywhere submits (or retries);
    the IME auto-returns to the previous keyboard after committing.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/voice_panel"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/recognition_bg"
    android:clickable="true"
    android:focusable="true"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="8dp"
    android:paddingBottom="10dp">

    <ImageButton
        android:id="@+id/gear_button"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="end"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/cd_settings"
        android:padding="10dp"
        android:src="@drawable/ic_settings"
        app:tint="@color/recognition_fg" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center">

        <View
            android:id="@+id/voice_ring"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:background="@drawable/voice_ring"
            android:alpha="0" />

        <ImageView
            android:id="@+id/mic_icon"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:contentDescription="@string/cd_mic"
            android:src="@drawable/ic_mic"
            app:tint="@color/recognition_fg" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="16dp"
            android:gravity="center"
            android:orientation="vertical">

            <TextView
                android:id="@+id/status_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/recognition_fg"
                android:textSize="18sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/hint_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:gravity="center"
                android:maxLines="2"
                android:textColor="@color/recognition_fg_dim"
                android:textSize="14sp" />
        </LinearLayout>
    </FrameLayout>
</LinearLayout>
