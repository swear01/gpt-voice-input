<?xml version="1.0" encoding="utf-8"?>
<!--
    Voice-only input view for the IME (v1.0.0): no keys. Tap anywhere on the
    panel to switch to the next input method; Done (✓) or auto-stop submits
    the transcription; the gear opens Settings.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/voice_panel"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/recognition_bg"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="8dp"
    android:paddingBottom="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end">

        <Button
            android:id="@+id/done_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:text="@string/done"
            android:visibility="gone" />

        <ImageButton
            android:id="@+id/gear_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_settings"
            android:padding="10dp"
            android:src="@drawable/ic_settings"
            app:tint="@color/recognition_fg_dim" />
    </LinearLayout>

    <ImageView
        android:id="@+id/mic_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginTop="4dp"
        android:contentDescription="@string/cd_mic"
        android:src="@drawable/ic_mic"
        app:tint="@color/recognition_fg" />

    <LinearLayout
        android:id="@+id/mic_level_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:visibility="gone">

        <View style="@style/MicBar" />
        <View style="@style/MicBar" />
        <View style="@style/MicBar" />
        <View style="@style/MicBar" />
        <View style="@style/MicBar" />
        <View style="@style/MicBar" />
        <View style="@style/MicBar" />
    </LinearLayout>

    <TextView
        android:id="@+id/status_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:textColor="@color/recognition_fg"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/hint_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:gravity="center"
        android:maxLines="2"
        android:textColor="@color/recognition_fg_dim"
        android:textSize="14sp" />

    <LinearLayout
        android:id="@+id/error_buttons"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal"
        android:visibility="gone">

        <Button
            android:id="@+id/cancel_button"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/cancel"
            android:textColor="@color/recognition_fg" />

        <Button
            android:id="@+id/open_settings_error_button"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/open_settings"
            android:textColor="@color/recognition_fg"
            android:visibility="gone" />

        <Button
            android:id="@+id/retry_button"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/retry"
            android:textColor="@color/recognition_fg" />
    </LinearLayout>

</LinearLayout>
