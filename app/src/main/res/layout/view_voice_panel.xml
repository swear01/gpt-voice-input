<?xml version="1.0" encoding="utf-8"?>
<!--
    Voice-only IME panel (v1.0.5). Minimal, whisperIME-style: a large mic
    indicator, the level meter, status/hint text, and a small gear. No Done
    button: tapping anywhere on the panel submits (or retries on error); the
    IME auto-returns to the previous keyboard after committing. System-bar /
    language-bar insets are applied in code.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/voice_panel"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/recognition_bg"
    android:clickable="true"
    android:focusable="true"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="8dp"
    android:paddingBottom="10dp">

    <ImageButton
        android:id="@+id/gear_button"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="end"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/cd_settings"
        android:padding="10dp"
        android:src="@drawable/ic_settings"
        app:tint="@color/recognition_fg_dim" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/mic_icon"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:contentDescription="@string/cd_mic"
            android:src="@drawable/ic_mic"
            app:tint="@color/recognition_fg" />

        <LinearLayout
            android:id="@+id/mic_level_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center"
            android:orientation="horizontal"
            android:visibility="gone">

            <View style="@style/MicBar" />
            <View style="@style/MicBar" />
            <View style="@style/MicBar" />
            <View style="@style/MicBar" />
            <View style="@style/MicBar" />
            <View style="@style/MicBar" />
            <View style="@style/MicBar" />
        </LinearLayout>

        <TextView
            android:id="@+id/status_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textColor="@color/recognition_fg"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/hint_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:gravity="center"
            android:maxLines="2"
            android:textColor="@color/recognition_fg_dim"
            android:textSize="14sp" />
    </LinearLayout>
</LinearLayout>
